spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
      hibernate.format_sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/nextusermngt
    username: akmal
    password: akmal

